# eu-einvoice-syntax-cii

CII semantic bindings, reading, writing and XSD execution. Require `eu_einvoice/syntax/cii`. Profiles and schema resources are supplied by a specification pack, not hardcoded country defaults.

Use the public `EuEinvoice::Client` assembled with a pack. This package adds no PDF or Rails dependencies.
