# Synthetic version-isolation fixtures

These small schemas and rule sets test resource selection and cache isolation. They are
not official invoice specifications. Both versions deliberately use the same CII namespace
and guideline. Alpha and beta accept different root attributes and emit distinct rule IDs
read from their adjacent code lists.
