# Contributing to facturx

## Setup

```bash
mise install
bundle install
```

## Checks

```bash
bundle exec rubocop
bundle exec rspec
bundle exec rake build
```

Create changes on a feature branch and submit them through a pull request to the protected `master` branch.
